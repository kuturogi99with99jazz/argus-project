# Argus エージェント作業指針

このファイルは、Argus プロジェクトで作業する開発エージェントが守るルールを定めます。

## プロジェクト概要

Argus は、登録した Web ページの更新有無をユーザーが手動で確認するための Windows / macOS デスクトップアプリです。

初期版では、以下の技術を使用します。

- OS: Windows 10 / Windows 11 x64、macOS 14 以降 Apple Silicon
- UI: Avalonia
- 言語: C#
- フレームワーク: .NET 8 以降
- 保存形式: JSON

初期版では、自動監視、常駐処理、通知機能を実装しません。

## 仕様書の所在と優先順位

仕様および計画は、以下の文書で管理します。

- 要件: `Design/requirement.md`
- 設計: `Design/design.md`
- タスク: `Design/tasks.md`
- プロジェクト構想: `Design/project-overview.md`

文書や指示の内容が矛盾する場合は、以下の順に優先します。

1. ユーザーの最新指示
2. `Design/requirement.md`
3. `Design/design.md`
4. `Design/tasks.md`
5. `Design/project-overview.md`

矛盾や解釈によって実装結果が大きく変わる曖昧さを見つけた場合は、勝手に決定せずユーザーへ確認してください。

## SDD と TDD の進め方

本プロジェクトは、仕様駆動開発（SDD）とテスト駆動開発（TDD）を組み合わせて進めます。

SDD では「何を作るか」を要件、設計、タスクで定義し、TDD では「定義した振る舞いをどう安全に実装するか」をテストで駆動します。

1. 実装前に `Design/requirement.md` で要件を定義する
2. 要件に基づいて `Design/design.md` に設計を記載する
3. 設計に基づいて `Design/tasks.md` に、受け入れ条件と必要なテストを含む実装タスクを分解する
4. `Design/tasks.md` から、独立して検証できる最小のタスクを一つ選ぶ
5. Red: 要件上の期待する振る舞いを表す、失敗するテストを先に作成する
6. Green: そのテストを通すために必要な最小限の実装を行う
7. Refactor: 全テストが成功する状態を維持しながら、設計とコードを改善する
8. 対象タスクのテストと影響範囲のテストを実行する
9. 仕様、設計、タスク、実装、テストの整合を確認し、タスクの状態を更新する

以下のルールを守ってください。

- `Design/tasks.md` に存在しない機能を勝手に実装しない
- 仕様変更時は、コードより先に関連する仕様文書を更新する
- 要件、設計、タスク、実装、テストの間に不整合を残さない
- 将来予定の機能を、現在のタスクへ無断で含めない
- 実装中に新しい判断が必要になった場合は、該当する仕様文書へ反映する
- テスト可能な振る舞いを追加または変更する場合は、原則としてプロダクションコードより先にテストを変更する
- Red では、テストが意図した理由で失敗することを確認する
- Green では、現在のテストを通す範囲を超えた先行実装を行わない
- Refactor では、外部から観測できる振る舞いを変えない
- 不具合修正では、可能な限り不具合を再現する失敗テストを先に追加する
- ドキュメント、生成コード、単純なプロジェクト設定など、テスト先行が適さない変更に無理なテストを追加しない
- テストを先に作成できない場合は、その理由と代替の検証方法を `Design/tasks.md` に記録する

## アーキテクチャ方針

- UI と更新チェック処理を分離する
- Avalonia の View とコードビハインドに業務ロジックを置かない
- UI は MVVM を基本とし、画面状態と操作を ViewModel に置く
- HTML の取得、正規化、比較、保存を個別の責務に分ける
- Core 層から Avalonia 層へ依存させない
- 将来 UI を変更しても Core 層を再利用できる構成にする
- 外部サービスやファイルシステムへのアクセスは、必要に応じてインターフェースで抽象化する
- クラスやメソッドに複数の責務を持たせない

## 実装ルール

- Nullable を有効のまま維持する
- 非同期 I/O には `async` / `await` を使用する
- HTTP 通信のたびに新しい `HttpClient` を生成しない
- 非同期処理では、可能な範囲で `CancellationToken` を受け渡す
- チェック中にエラーが発生した場合は、前回の正常なデータを上書きしない
- JSON の保存形式と文字コードを統一する
- 秘密情報をソースコード、ログ、保存用 JSON に含めない
- 例外を理由なく握りつぶさない
- 警告を安易に抑制せず、原因を解消する
- 新しい外部パッケージは、必要性と影響を確認してから追加する

## 命名とコードスタイル

- 型、メソッド、プロパティには `PascalCase` を使用する
- ローカル変数と引数には `camelCase` を使用する
- インターフェースには `I` 接頭辞を付ける
- 非同期メソッドには `Async` 接尾辞を付ける
- 名前から役割が分かる、具体的な命名を使用する
- クラス、インターフェース、メソッド、プロパティには XML サマリーコメントを記述する
- テストプロジェクト内の手書きコードもコメント記述の対象とする
- XML サマリーコメントは日本語で記述し、体言止めで文末に「。」を付けない
- XML サマリーコメントには、処理内容の単なる言い換えではなく、役割や実装上の理由を記載する
- 既存コードのスタイルを尊重し、無関係な整形変更を混在させない
- C#ソースの改行コードはCRLFに統一する
- XAML のコードビハインドには、View 固有のイベント接続以外の処理を置かない

## テスト方針

- テストフレームワークとテストプロジェクトの構成は `Design/design.md` で決定する
- 各テストは、対応する要件またはタスク上の振る舞いを明確に示す
- 一つのテストでは、原則として一つの振る舞いを検証する
- テスト名から、前提、操作、期待結果が読み取れるようにする
- テストは独立して実行でき、実行順序や他のテストの結果へ依存させない
- 時刻、乱数、HTTP、ファイルシステムなど不安定な外部要因は制御または抽象化する
- Core と ViewModel の処理は UI なしでテストできるようにする
- HTML の正規化処理と比較処理には単体テストを用意する
- 少なくとも、初回取得、更新なし、更新あり、通信エラーを検証する
- エラー時に前回データが保持されることを検証する
- テストを実際の Web サイトの状態へ依存させない
- Web ページ取得処理のテストでは、保存済み HTML、スタブ、モックなどを使用する
- 不具合を修正する場合は、可能な限り再現テストを追加する
- テストを通すためだけに、本番の振る舞いを歪める分岐を追加しない
- テスト失敗を解消せずに、テストの削除、無効化、期待値の安易な変更を行わない

## ビルドと検証

基本的な検証コマンドは以下です。

```powershell
dotnet restore
dotnet build Argus.sln
dotnet test Argus.sln
```

作業完了前に、変更内容に応じた検証を行ってください。

- ソースコードを変更した場合はビルドする
- テストが存在する場合は関連テストを実行する
- 共通処理や影響範囲の広い処理を変更した場合は全テストを実行する
- 警告やテスト失敗が残る場合は、その内容と理由を報告する
- 実行できなかった検証がある場合は、その理由を報告する

## Git 管理

- `bin/`、`obj/`、`.vs/` などの生成物は Git で追跡しない
- 既に追跡されている生成物を整理する場合は、ファイル本体を削除せず `git rm --cached` でインデックスから外す
- 生成物の追跡解除や `.gitignore` の更新は、将来の再生成を前提に行う

## 初期版で対応しないこと

明示的な仕様変更がない限り、初期版では以下を実装しません。

- 自動チェック
- 常駐監視
- スケジュール実行
- LINE、Slack、メールなどへの通知
- ログイン必須サイトの監視
- CAPTCHA が必要なサイトの監視
- JavaScript 実行後の SPA 解析
- Playwright などによるブラウザレンダリング
- 外部データベースとの連携
- Wails、Tauri など別UIフレームワークへの移行

## 作業完了条件

作業を完了とする前に、以下を確認してください。

- 対応する要件を満たしている
- `Design/requirement.md` と実装が一致している
- `Design/design.md` と実装が一致している
- `Design/tasks.md` の対象タスクが更新されている
- ビルドが成功している
- 要件と受け入れ条件に対応するテストが先行して追加されている、またはテスト先行できない理由が記録されている
- 新規および既存の必要なテストがすべて成功している
- エラー時に既存の正常なデータを破壊しない
- README や仕様文書に影響する変更が反映されている
- 対応範囲外の変更が混在していない
